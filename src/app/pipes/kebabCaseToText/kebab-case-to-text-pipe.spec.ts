import { KebabCaseToTextPipe } from './kebab-case-to-text-pipe';

describe('KebabCaseToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new KebabCaseToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
